go test -coverprofile=coverage.out  => gera um arquivo e mostra no terminal a cobertura de test
go tool cover -html=coverage.out    => abre no browser o codigo coberto e o arquivo .out deve existir
go test -bench=.
go test -bench=. -run=^#
go test -bench=. -run=^# -benchmem
go test -bench=. -run=^# -count=10 -benchtime=5s



****************** BANCO DE DADOS MYSQL ******************
mysql -uroot -p safisa 

create table usuarios(id varchar(255), nome varchar(255), senha varchar(255), ativo bool)
create table fornecedores(id varchar(255), razao_social varchar(255), nome_fantasia varchar(255), cnpj varchar(20), ie varchar(30), ativo bool)