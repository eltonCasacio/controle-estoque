version: '3'

services:
  mysql:
    image: mysql:5.7
    container_name: mysql
    restart: always
    platform: linux/amd64
    environment:
      MSQL_ROOT_PASSWORD: root
      MSQL_DATABASE: safisa
      MSQL_PASSWORD: root
    ports:
      - 3306:3306
      
